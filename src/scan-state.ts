export let lastPhashThreshold = 8;

export function setLastPhashThreshold(v: number) {
  lastPhashThreshold = v;
}

export let lastFolderPriorities: string[] = [];

export function setLastFolderPriorities(v: string[]) {
  lastFolderPriorities = v;
}
